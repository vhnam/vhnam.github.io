---
import dayjs from "dayjs";

export interface Props {
	pubDate: Date;
}

const { pubDate } = Astro.props;
---

<time
	class="text-xs text-primary-700/75 md:text-sm dark:text-primary-400/85"
	datetime={new Date(pubDate).toISOString()}
>
	{dayjs(pubDate).format("DD/MM/YYYY")}
</time>
