---
import dayjs from "dayjs";

export interface Props {
	pubDate: Date;
}

const { pubDate } = Astro.props;
---

<time class="text-sm" datetime={new Date(pubDate).toISOString()}>
	{dayjs(pubDate).format("DD/MM/YYYY")}
</time>
